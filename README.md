# ralph
For testing the Ralph AI agent loop.
## Running on Windows (PowerShell)

```powershell
.\scripts\ralph.ps1
```

You can point to a custom codex binary by setting `CODEX_BIN`:

```powershell
$env:CODEX_BIN = "C:\Path\To\codex.exe"
.\scripts\ralph.ps1
```
 
